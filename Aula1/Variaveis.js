

/*

    let

A palavra-chave let é usada para declarar variáveis que podem ter seu valor alterado ao longo do código. Ela tem escopo de bloco, o que significa que a variável só existe dentro do bloco onde foi declarada. Veja o exemplo:

JavaScript

let idade = 20; // declaração da variável
console.log(idade); // imprime 20

idade = 21; // alteração do valor
console.log(idade); // imprime 21
Código gerado por IA. Examine e use com cuidado. Mais informações em perguntas frequentes.
const

Já a palavra-chave const é usada para declarar constantes, ou seja, variáveis que não podem ter seu valor alterado depois de declaradas. Assim como o let, o const também tem escopo de bloco. Veja o exemplo:

JavaScript

const pi = 3.14; // declaração da constante
console.log(pi); // imprime 3.14

pi = 3.1415; // tentativa de alteração do valor
console.log(pi); // Erro! Não é possível alterar o valor de uma constante


*/
let carro  = 'ferrari'
carro = 'fiat'

const nome = "Rafael042";
const idade = 52;
const sabejavascript= true;

console.log(nome,idade,sabejavascript);

